services:
  postgres:
    image: timescale/timescaledb:latest-pg16
    container_name: ups-postgres
    environment:
      POSTGRES_DB: ups_monitoring
      POSTGRES_USER: ups
      POSTGRES_PASSWORD: ups_password
    ports:
      - "5432:5432"
